#include<bits/stdc++.h>

using namespace std;

int fun(string blocked, string unblocked){
	vector <string> prefix,c;
	int flag=0;
	if (blocked.length()==0){
		cout<<0<<endl;
		return;

	}
	else{
		for (int i=0;i<blocked.length();i++){
		
		
		}
		
	}

}
int main()
{
	int n;
	cin>>n;
	vector <string> v1,v2,v3;
	string a[n];char b[n];
	fn(i,n)
	{string s;
	cin>>b[i];
		cin>>s;	
		v1.pb(s);
	}
	fn(i,v1.size())
	{
		if(b[i]=='-')
		v3.push_back(v1[i]);
		else
		v2.push_back(v1[i]);
	}
	return 0;
	}

